# [模块名] 契约(Contracts)

**功能(Feature)**: [###-feature-name]
**日期(Date)**: [DATE]
**版本(Version)**: [版本号]

> [本文档说明，如引用上游契约文档]

---

## 契约(Contract): `[名称]` ([状态: 新增(NEW) / 修改(MODIFIED)])

**调用(Call)**: `[调用方式和参数]`

**默认值(Defaults)**:
| 参数(Parameter) | 默认值(Default) |
|-----------------|----------------|
| [参数 1] | [值] |

**前置条件(Preconditions)**: [前置条件说明]

**行为(Behavior)**:
1. [步骤 1]
2. [步骤 2]
3. [步骤 3]

**变更说明(Changes from previous)**:

| 组件(Component) | 旧值(Before) | 新值(After) |
|-----------------|-------------|-------------|
| [组件] | [旧值] | [新值] |

**并行执行(Parallel Execution)**: [说明]

**退出码(Exit codes)**:

| 退出码(Code) | 含义(Meaning) |
|-------------|---------------|
| 0 | 成功(Success) |
| 1 | [失败原因] |

---

## 变更总结(Summary of Changes)

| 契约(Contract) | 状态(Status) |
|---------------|-------------|
| `[名称]` | 新增(NEW) / 修改(MODIFIED) / 未变更(UNCHANGED) |
